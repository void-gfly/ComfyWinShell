<UserControl x:Class="WpfDesktop.Views.ProfileManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="20" Background="#050505">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar: Create New Profile -->
        <Border Grid.Row="0" Margin="0,0,0,20" Padding="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="名称:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Grid.Column="1" Text="{Binding NewProfileName, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,0,20,0"/>

                <TextBlock Grid.Column="2" Text="描述:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Grid.Column="3" Text="{Binding NewProfileDescription, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,0,20,0"/>

                <Button Grid.Column="4" Content="新建配置" Command="{Binding CreateCommand}" Style="{DynamicResource PrimaryButtonStyle}"/>
            </Grid>
        </Border>

        <!-- List: Profiles -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Profiles}"
                  SelectedItem="{Binding SelectedProfile}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="描述" Binding="{Binding Description}" Width="*"/>
                <DataGridCheckBoxColumn Header="默认" Binding="{Binding IsDefault}" Width="60" IsReadOnly="True"/>
                <DataGridTextColumn Header="修改时间" Binding="{Binding LastModified, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="160"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Bottom: Actions & Status -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Status Message -->
            <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" VerticalAlignment="Center" Opacity="0.7"/>

            <!-- Action Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="刷新" Command="{Binding RefreshCommand}" Style="{DynamicResource SecondaryButtonStyle}" Margin="0,0,10,0"/>
                <Button Content="导入" Command="{Binding ImportCommand}" Style="{DynamicResource SecondaryButtonStyle}" Margin="0,0,10,0"/>
                <Button Content="导出" Command="{Binding ExportCommand}" Style="{DynamicResource SecondaryButtonStyle}" Margin="0,0,10,0"/>
                <Button Content="设为默认" Command="{Binding SetDefaultCommand}" Style="{DynamicResource PrimaryButtonStyle}" Margin="0,0,10,0"/>
                <Button Content="删除" Command="{Binding DeleteCommand}" Style="{DynamicResource DangerButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
