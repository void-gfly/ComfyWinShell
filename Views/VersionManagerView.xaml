<UserControl x:Class="WpfDesktop.Views.VersionManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:WpfDesktop.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="20" Background="#050505">
        <!-- ComfyUI Found - Show Git Info -->
        <Grid Visibility="{Binding IsComfyUiFound, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Header with Git Info -->
            <Border Grid.Row="0"
                    BorderThickness="0,0,0,1"
                    BorderBrush="#333"
                    Padding="0,0,0,20"
                    Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="ComfyUI 路径:"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       Width="100"/>
                            <TextBlock Text="{Binding ComfyUiPath}"
                                       Foreground="{DynamicResource PrimaryTextBrush}"
                                       TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding ComfyUiPath}"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="Git 远程仓库:"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       Width="100"/>
                            <TextBlock Text="{Binding RemoteUrl}"
                                       Foreground="{DynamicResource PrimaryTextBrush}"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="当前分支:"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       Width="100"/>
                            <TextBlock Text="{Binding CurrentBranch}"
                                       Foreground="{DynamicResource AccentBrush}"
                                       FontWeight="SemiBold"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="当前提交:"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       Width="100"/>
                            <TextBlock Text="{Binding CurrentVersionHash}"
                                       Foreground="{DynamicResource PrimaryTextBrush}"
                                       FontFamily="Consolas"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1" VerticalAlignment="Top">
                        <Button Content="刷新"
                                Command="{Binding RefreshCommand}"
                                Style="{DynamicResource SecondaryButtonStyle}"
                                Width="80"/>
                        <TextBlock Text="{Binding StatusMessage}"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   HorizontalAlignment="Right"
                                   Margin="0,10,0,0"
                                   FontSize="12"
                                   MaxWidth="200"
                                   TextWrapping="Wrap"
                                   TextAlignment="Right"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Tab Controls -->
            <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                        <Setter Property="Padding" Value="15,8"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border Name="Border" BorderThickness="0,0,0,2" BorderBrush="Transparent" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>

                <TabItem Header="稳定版本 (Tags)">
                    <ListView ItemsSource="{Binding StableVersions}"
                              BorderThickness="0"
                              Background="Transparent"
                              Margin="0,10,0,0">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="标签" Width="150">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Tag}" FontWeight="SemiBold" Foreground="{DynamicResource AccentBrush}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="提交哈希" Width="100" DisplayMemberBinding="{Binding ShortHash}"/>
                                <GridViewColumn Header="日期" Width="150" DisplayMemberBinding="{Binding Date, StringFormat={}{0:yyyy-MM-dd HH:mm}}"/>
                                <GridViewColumn Header="操作" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Command="{Binding DataContext.SwitchVersionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Padding="10,2"
                                                    FontSize="11">
                                                <Button.Style>
                                                    <Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                                                        <Setter Property="Content" Value="切换"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                                                <Setter Property="Content" Value="当前"/>
                                                                <Setter Property="IsEnabled" Value="False"/>
                                                                <Setter Property="Opacity" Value="0.5"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </TabItem>

                <TabItem Header="开发版本 (Commits)">
                    <ListView ItemsSource="{Binding DevVersions}"
                              BorderThickness="0"
                              Background="Transparent"
                              Margin="0,10,0,0">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="提交哈希" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding ShortHash}" FontFamily="Consolas" Foreground="{DynamicResource AccentBrush}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="消息" Width="300">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Message}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Message}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="日期" Width="150" DisplayMemberBinding="{Binding Date, StringFormat={}{0:yyyy-MM-dd HH:mm}}"/>
                                <GridViewColumn Header="操作" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Command="{Binding DataContext.SwitchVersionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Padding="10,2"
                                                    FontSize="11">
                                                <Button.Style>
                                                    <Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                                                        <Setter Property="Content" Value="切换"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                                                <Setter Property="Content" Value="当前"/>
                                                                <Setter Property="IsEnabled" Value="False"/>
                                                                <Setter Property="Opacity" Value="0.5"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </TabItem>
            </TabControl>
        </Grid>

        <!-- ComfyUI Not Found - Show Guidance -->
        <Grid Visibility="{Binding IsComfyUiFound, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Warning Icon and Title -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,40,0,30">
                <TextBlock Text="⚠️" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                <TextBlock Text="未找到 ComfyUI"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Guidance Message -->
            <Border Grid.Row="1"
                    Background="#111"
                    CornerRadius="8"
                    Padding="30"
                    Margin="50,0"
                    VerticalAlignment="Top">
                <TextBlock Text="{Binding NotFoundMessage}"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           FontFamily="Consolas"
                           FontSize="13"
                           LineHeight="22"
                           TextWrapping="Wrap"/>
            </Border>

            <!-- Refresh Button -->
            <StackPanel Grid.Row="2" HorizontalAlignment="Center" Margin="0,30">
                <Button Content="重新检测"
                        Command="{Binding RefreshCommand}"
                        Style="{DynamicResource PrimaryButtonStyle}"
                        Padding="30,12"/>
                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,15,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Loading Overlay -->
        <Grid Background="#AA000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="正在处理..." Foreground="White" FontSize="16"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
