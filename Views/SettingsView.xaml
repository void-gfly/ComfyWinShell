<UserControl x:Class="WpfDesktop.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:WpfDesktop.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900"
             d:DataContext="{d:DesignInstance vm:SettingsViewModel}">

    <UserControl.Resources>
        <!-- Card Style -->
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="#141414"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="TextElement.Foreground" Value="{DynamicResource TextBrush}"/>
        </Style>

        <Style x:Key="SectionExpander" TargetType="Expander">
            <Setter Property="IsExpanded" Value="False"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}"
                                   Foreground="#D4AF37"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Margin="0,0,0,8"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Expander}">
                        <DockPanel>
                            <ToggleButton x:Name="HeaderSite"
                                          DockPanel.Dock="Top"
                                          Content="{TemplateBinding Header}"
                                          ContentTemplate="{TemplateBinding HeaderTemplate}"
                                          IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Padding="{TemplateBinding Padding}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent" Padding="{TemplateBinding Padding}">
                                            <Grid Background="Transparent" SnapsToDevicePixels="False">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <!-- Custom Gold Toggle Arrow -->
                                                <Border x:Name="Circle"
                                                        Width="24"
                                                        Height="24"
                                                        CornerRadius="12"
                                                        BorderBrush="#D4AF37"
                                                        BorderThickness="1"
                                                        Background="Transparent"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"
                                                        Margin="0,0,10,0">
                                                    <Path x:Name="Arrow"
                                                          Data="M 0,0 L 4,4 L 8,0"
                                                          Stroke="#D4AF37"
                                                          StrokeThickness="2"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          RenderTransformOrigin="0.5,0.5">
                                                        <Path.RenderTransform>
                                                            <RotateTransform Angle="-90"/>
                                                        </Path.RenderTransform>
                                                    </Path>
                                                </Border>
                                                
                                                <ContentPresenter Grid.Column="1"
                                                                  VerticalAlignment="Center"
                                                                  RecognizesAccessKey="True"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="Arrow" Property="RenderTransform">
                                                    <Setter.Value>
                                                        <RotateTransform Angle="0"/>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter TargetName="Circle" Property="Background" Value="#20D4AF37"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Circle" Property="BorderBrush" Value="#FFD700"/>
                                                <Setter TargetName="Arrow" Property="Stroke" Value="#FFD700"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ExpandSite"
                                              DockPanel.Dock="Bottom"
                                              Focusable="false"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Visibility="Collapsed"/>
                        </DockPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ExpandSite" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

<Grid Background="#050505">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Fixed Top Header -->
        <Border Grid.Row="0" Margin="0" Padding="80,16" BorderBrush="#2A2A2A" BorderThickness="0,0,0,1" Background="#050505" Panel.ZIndex="100">
            <Grid MaxWidth="1000" HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Title Section -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="设置" FontSize="24" FontWeight="Bold" Margin="0,0,16,0" VerticalAlignment="Center"/>
                    <TextBlock Text="管理 ComfyShell 的外观、行为与存储配置" FontSize="14" Opacity="0.6" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Save Action - Moved to Top -->
                <Button Grid.Column="1" 
                        Command="{Binding SaveCommand}"
                        Content="保存配置"
                        Width="120" Height="40"
                        VerticalAlignment="Center"
                        Style="{DynamicResource PrimaryButtonStyle}"/>
            </Grid>
        </Border>

        <!-- Scrollable Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Background="#050505">
            <StackPanel Margin="24" MaxWidth="1200" HorizontalAlignment="Stretch">
                
                <!-- General Section -->
                <Expander Header="常规 (General)" Style="{StaticResource SectionExpander}" IsExpanded="True">
                    <Border Style="{StaticResource CardBorder}">
                        <StackPanel>
                            <!-- App Name -->
                            <StackPanel Margin="0,0,0,25">
                                <TextBlock Text="应用程序名称" FontSize="14" FontWeight="Medium" Opacity="0.7" Margin="0,0,0,8"/>
                                <TextBox Text="{Binding Settings.AppName, UpdateSourceTrigger=PropertyChanged}"
                                         Width="250" HorizontalAlignment="Left"
                                         VerticalContentAlignment="Center"/>
                                <TextBlock Text="自定义窗口标题显示的名称"
                                           FontSize="12" Opacity="0.5" Margin="0,6,0,0"/>
                            </StackPanel>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="40"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Language -->
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="界面语言" FontSize="14" FontWeight="Medium" Opacity="0.7" Margin="0,0,0,8"/>
                                    <ComboBox ItemsSource="{Binding LanguageOptions}"
                                              SelectedItem="{Binding Settings.Language}"/>
                                    <TextBlock Text="应用程序的主要显示语言"
                                               FontSize="12" Opacity="0.5" Margin="0,6,0,0"/>
                                </StackPanel>

                                <!-- Theme -->
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="颜色主题" FontSize="14" FontWeight="Medium" Opacity="0.7" Margin="0,0,0,8"/>
                                    <ComboBox ItemsSource="{Binding ThemeOptions}"
                                              SelectedItem="{Binding Settings.Theme}"/>
                                    <TextBlock Text="选择深色或浅色界面风格"
                                               FontSize="12" Opacity="0.5" Margin="0,6,0,0"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Expander>
                
                <!-- Console Section -->
                <Expander Header="控制台 (Console)" Style="{StaticResource SectionExpander}">
                    <Border Style="{StaticResource CardBorder}">
                        <StackPanel>
                            <TextBlock Text="最大日志行数" FontSize="14" FontWeight="Medium" Opacity="0.7" Margin="0,0,0,8"/>
                            <TextBox Text="{Binding Settings.MaxLogLines, UpdateSourceTrigger=PropertyChanged}"
                                     Width="150" HorizontalAlignment="Left"
                                     VerticalContentAlignment="Center"/>
                            <TextBlock Text="控制台输出保留的最大行数，超出后自动删除旧日志。默认 5000 行。"
                                       FontSize="12" Opacity="0.5" Margin="0,6,0,16"/>

                            <TextBlock Text="输出文本间距" FontSize="14" FontWeight="Medium" Opacity="0.7" Margin="0,0,0,8"/>
                            <ComboBox ItemsSource="{Binding LogLineHeightOptions}"
                                      DisplayMemberPath="Label"
                                      SelectedValuePath="Value"
                                      SelectedValue="{Binding Settings.LogLineHeight, UpdateSourceTrigger=PropertyChanged}"
                                      Width="150" HorizontalAlignment="Left"/>
                            <TextBlock Text="控制台日志行高：紧凑=12，默认=15，宽松=18。"
                                       FontSize="12" Opacity="0.5" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                </Expander>

                <!-- Proxy Section -->
                <Expander Header="网络代理 (Proxy)" Style="{StaticResource SectionExpander}">
                    <Border Style="{StaticResource CardBorder}">
                        <StackPanel>
                             <CheckBox IsChecked="{Binding Settings.Proxy.Enabled}" Margin="0,0,0,15">
                                <TextBlock Text="启用代理服务器" FontSize="14" FontWeight="Medium"/>
                            </CheckBox>
                            <TextBlock Text="启用后，ComfyUI、Git、pip 等所有网络操作都将通过代理服务器"
                                       FontSize="12" Opacity="0.5" Margin="0,0,0,15"/>

                            <TextBlock Text="代理服务器地址" FontSize="14" FontWeight="Medium" Opacity="0.7" Margin="0,0,0,8"/>
                            <TextBox Text="{Binding Settings.Proxy.Server, UpdateSourceTrigger=PropertyChanged}"
                                     IsEnabled="{Binding Settings.Proxy.Enabled}"
                                     Width="350" HorizontalAlignment="Left"
                                     VerticalContentAlignment="Center"/>
                            <TextBlock Text="示例: http://127.0.0.1:7890 或 http://proxy.example.com:8080"
                                       FontSize="12" Opacity="0.5" Margin="0,6,0,15"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="40"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="用户名 (可选)" FontSize="14" FontWeight="Medium" Opacity="0.7" Margin="0,0,0,8"/>
                                    <TextBox Text="{Binding Settings.Proxy.Username, UpdateSourceTrigger=PropertyChanged}"
                                             IsEnabled="{Binding Settings.Proxy.Enabled}"
                                             VerticalContentAlignment="Center"/>
                                    <TextBlock Text="代理服务器需要认证时填写"
                                               FontSize="12" Opacity="0.5" Margin="0,6,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="密码 (可选)" FontSize="14" FontWeight="Medium" Opacity="0.7" Margin="0,0,0,8"/>
                                    <PasswordBox x:Name="ProxyPasswordBox"
                                                 IsEnabled="{Binding Settings.Proxy.Enabled}"
                                                 PasswordChanged="ProxyPasswordBox_OnPasswordChanged"
                                                 VerticalContentAlignment="Center"/>
                                    <TextBlock Text="密码将加密存储"
                                               FontSize="12" Opacity="0.5" Margin="0,6,0,0"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Expander>

                <!-- Mirror Settings -->
                <Expander Header="镜像加速 (Mirrors)" Style="{StaticResource SectionExpander}">
                    <Border Style="{StaticResource CardBorder}">
                        <StackPanel>
                            <!-- GitHub Mirror -->
                            <StackPanel Margin="0,0,0,30">
                                <CheckBox IsChecked="{Binding Settings.GitHubMirror.Enabled}" Margin="0,0,0,15">
                                    <TextBlock Text="启用 GitHub 镜像站点" FontSize="14" FontWeight="Medium"/>
                                </CheckBox>
                                <TextBlock Text="加速 Git 克隆、自定义节点下载等 GitHub 操作"
                                           FontSize="12" Opacity="0.5" Margin="0,0,0,15"/>

                                <TextBlock Text="GitHub 镜像地址" FontSize="14" FontWeight="Medium" Opacity="0.7" Margin="0,0,0,8"/>
                                <TextBox Text="{Binding Settings.GitHubMirror.MirrorUrl, UpdateSourceTrigger=PropertyChanged}"
                                         IsEnabled="{Binding Settings.GitHubMirror.Enabled}"
                                         HorizontalAlignment="Stretch"
                                         VerticalContentAlignment="Center"/>
                                
                                <TextBlock Text="常用镜像:" FontSize="12" Opacity="0.5" Margin="0,8,0,6"/>
                                <StackPanel>
                                    <!-- Mirror 1 -->
                                    <Border BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="4" Padding="8,6" Margin="0,0,0,6" Background="#0A0A0A">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="https://ghproxy.com/https://github.com" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" FontFamily="Consolas"/>
                                                <TextBlock Text="推荐 - 稳定性好" FontSize="11" Opacity="0.5" Margin="0,2,0,0"/>
                                            </StackPanel>
                                            <Button Grid.Column="1" Click="CopyGitHubMirror1_Click" Style="{DynamicResource SecondaryButtonStyle}" Padding="8,4" ToolTip="复制到剪贴板">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="📋" FontSize="14" Margin="0,0,4,0"/>
                                                    <TextBlock Text="复制" FontSize="11"/>
                                                </StackPanel>
                                            </Button>
                                        </Grid>
                                    </Border>
                                    <!-- Mirror 2 -->
                                    <Border BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="4" Padding="8,6" Margin="0,0,0,6" Background="#0A0A0A">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="https://mirror.ghproxy.com/https://github.com" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" FontFamily="Consolas"/>
                                                <TextBlock Text="备用镜像" FontSize="11" Opacity="0.5" Margin="0,2,0,0"/>
                                            </StackPanel>
                                            <Button Grid.Column="1" Click="CopyGitHubMirror2_Click" Style="{DynamicResource SecondaryButtonStyle}" Padding="8,4" ToolTip="复制到剪贴板">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="📋" FontSize="14" Margin="0,0,4,0"/>
                                                    <TextBlock Text="复制" FontSize="11"/>
                                                </StackPanel>
                                            </Button>
                                        </Grid>
                                    </Border>
                                    <!-- Mirror 3 -->
                                    <Border BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="4" Padding="8,6" Margin="0,0,0,0" Background="#0A0A0A">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="https://gh.api.99988866.xyz/https://github.com" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" FontFamily="Consolas"/>
                                                <TextBlock Text="备用镜像" FontSize="11" Opacity="0.5" Margin="0,2,0,0"/>
                                            </StackPanel>
                                            <Button Grid.Column="1" Click="CopyGitHubMirror3_Click" Style="{DynamicResource SecondaryButtonStyle}" Padding="8,4" ToolTip="复制到剪贴板">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="📋" FontSize="14" Margin="0,0,4,0"/>
                                                    <TextBlock Text="复制" FontSize="11"/>
                                                </StackPanel>
                                            </Button>
                                        </Grid>
                                    </Border>
                                </StackPanel>
                            </StackPanel>

                            <!-- pip Mirror -->
                            <StackPanel>
                                <CheckBox IsChecked="{Binding Settings.PipMirror.Enabled}" Margin="0,0,0,15">
                                    <TextBlock Text="启用 pip 镜像源" FontSize="14" FontWeight="Medium"/>
                                </CheckBox>
                                <TextBlock Text="加速 Python 包下载（pip install）"
                                           FontSize="12" Opacity="0.5" Margin="0,0,0,15"/>

                                <TextBlock Text="pip 镜像源地址" FontSize="14" FontWeight="Medium" Opacity="0.7" Margin="0,0,0,8"/>
                                <TextBox Text="{Binding Settings.PipMirror.IndexUrl, UpdateSourceTrigger=PropertyChanged}"
                                         IsEnabled="{Binding Settings.PipMirror.Enabled}"
                                         HorizontalAlignment="Stretch"
                                         VerticalContentAlignment="Center"/>
                                
                                <TextBlock Text="常用镜像源:" FontSize="12" Opacity="0.5" Margin="0,8,0,6"/>
                                <StackPanel Margin="0,0,0,15">
                                    <!-- Mirror 1 -->
                                    <Border BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="4" Padding="8,6" Margin="0,0,0,6" Background="#0A0A0A">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="https://pypi.tuna.tsinghua.edu.cn/simple" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" FontFamily="Consolas"/>
                                                <TextBlock Text="清华大学 - 推荐" FontSize="11" Opacity="0.5" Margin="0,2,0,0"/>
                                            </StackPanel>
                                            <Button Grid.Column="1" Click="CopyPipMirror1_Click" Style="{DynamicResource SecondaryButtonStyle}" Padding="8,4" ToolTip="复制到剪贴板">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="📋" FontSize="14" Margin="0,0,4,0"/>
                                                    <TextBlock Text="复制" FontSize="11"/>
                                                </StackPanel>
                                            </Button>
                                        </Grid>
                                    </Border>
                                    <!-- Mirror 2 -->
                                    <Border BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="4" Padding="8,6" Margin="0,0,0,6" Background="#0A0A0A">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="https://mirrors.aliyun.com/pypi/simple" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" FontFamily="Consolas"/>
                                                <TextBlock Text="阿里云" FontSize="11" Opacity="0.5" Margin="0,2,0,0"/>
                                            </StackPanel>
                                            <Button Grid.Column="1" Click="CopyPipMirror2_Click" Style="{DynamicResource SecondaryButtonStyle}" Padding="8,4" ToolTip="复制到剪贴板">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="📋" FontSize="14" Margin="0,0,4,0"/>
                                                    <TextBlock Text="复制" FontSize="11"/>
                                                </StackPanel>
                                            </Button>
                                        </Grid>
                                    </Border>
                                    <!-- Mirror 3 -->
                                    <Border BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="4" Padding="8,6" Margin="0,0,0,6" Background="#0A0A0A">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="https://pypi.mirrors.ustc.edu.cn/simple" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" FontFamily="Consolas"/>
                                                <TextBlock Text="中国科学技术大学" FontSize="11" Opacity="0.5" Margin="0,2,0,0"/>
                                            </StackPanel>
                                            <Button Grid.Column="1" Click="CopyPipMirror3_Click" Style="{DynamicResource SecondaryButtonStyle}" Padding="8,4" ToolTip="复制到剪贴板">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="📋" FontSize="14" Margin="0,0,4,0"/>
                                                    <TextBlock Text="复制" FontSize="11"/>
                                                </StackPanel>
                                            </Button>
                                        </Grid>
                                    </Border>
                                    <!-- Mirror 4 -->
                                    <Border BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="4" Padding="8,6" Margin="0,0,0,0" Background="#0A0A0A">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="https://mirrors.cloud.tencent.com/pypi/simple" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" FontFamily="Consolas"/>
                                                <TextBlock Text="腾讯云" FontSize="11" Opacity="0.5" Margin="0,2,0,0"/>
                                            </StackPanel>
                                            <Button Grid.Column="1" Click="CopyPipMirror4_Click" Style="{DynamicResource SecondaryButtonStyle}" Padding="8,4" ToolTip="复制到剪贴板">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="📋" FontSize="14" Margin="0,0,4,0"/>
                                                    <TextBlock Text="复制" FontSize="11"/>
                                                </StackPanel>
                                            </Button>
                                        </Grid>
                                    </Border>
                                </StackPanel>

                                <CheckBox IsChecked="{Binding Settings.PipMirror.TrustedHost}"
                                          IsEnabled="{Binding Settings.PipMirror.Enabled}"
                                          Margin="0,0,0,0">
                                    <TextBlock Text="信任该镜像源（避免 SSL 证书验证问题）" FontSize="13" Opacity="0.8"/>
                                </CheckBox>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Expander>

                <!-- Storage Section -->
                <Expander Header="存储与路径 (Storage)" Style="{StaticResource SectionExpander}">
                    <Border Style="{StaticResource CardBorder}">
                        <StackPanel>
                            <TextBlock Text="ComfyUI 数据根目录" FontSize="14" FontWeight="Medium" Opacity="0.7" Margin="0,0,0,8"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0"
                                         Text="{Binding Settings.DataRoot, UpdateSourceTrigger=PropertyChanged}"
                                         VerticalContentAlignment="Center"/>

                                <Button Grid.Column="2"
                                        Command="{Binding BrowseDataRootCommand}"
                                        Content="浏览文件夹..."
                                        Style="{DynamicResource SecondaryButtonStyle}"/>
                            </Grid>

                            <TextBlock Text="此目录用于存储所有模型、节点、工作流及生成的图像。更改后可能需要重启应用。"
                                       FontSize="12" Opacity="0.5" Margin="0,10,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </Expander>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
