<Window x:Class="WpfDesktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfDesktop"
        xmlns:viewmodels="clr-namespace:WpfDesktop.ViewModels"
        xmlns:views="clr-namespace:WpfDesktop.Views"
        mc:Ignorable="d"
        Title="{Binding AppTitle}" Height="700" Width="1100"
        WindowStartupLocation="CenterScreen"
        Closing="MainWindow_OnClosing"
        Background="#050505">

    <!-- 在 Window 级别定义 DataTemplate -->
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:DashboardViewModel}">
            <views:DashboardView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ConfigurationViewModel}">
            <views:ConfigurationView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:VersionManagerViewModel}">
            <views:VersionManagerView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ProfileManagerViewModel}">
            <views:ProfileManagerView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ProcessMonitorViewModel}">
            <views:ProcessMonitorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:HardwareMonitorViewModel}">
            <views:HardwareMonitorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:SettingsViewModel}">
            <views:SettingsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ResourcesViewModel}">
            <views:ResourcesView />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0"
                Background="#0A0A0A"
                BorderBrush="#2A2A2A"
                BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Navigation -->
                <StackPanel Grid.Row="1" Margin="10,0">
                    <RadioButton Content="首页" Tag="&#xE768;" Command="{Binding NavigateCommand}" CommandParameter="Dashboard" GroupName="NavGroup" IsChecked="True" Style="{DynamicResource NavRadioButtonStyle}"/>
                    <RadioButton Content="参数" Tag="&#xE9E9;" Command="{Binding NavigateCommand}" CommandParameter="Configuration" GroupName="NavGroup" Style="{DynamicResource NavRadioButtonStyle}"/>
                    <RadioButton Content="版本" Tag="&#xE81C;" Command="{Binding NavigateCommand}" CommandParameter="VersionManager" GroupName="NavGroup" Style="{DynamicResource NavRadioButtonStyle}"/>
                    <RadioButton Content="配置" Tag="&#xE7C3;" Command="{Binding NavigateCommand}" CommandParameter="ProfileManager" GroupName="NavGroup" Style="{DynamicResource NavRadioButtonStyle}"/>
                    <RadioButton Content="控制台" Tag="&#xE756;" Command="{Binding NavigateCommand}" CommandParameter="ProcessMonitor" GroupName="NavGroup" Style="{DynamicResource NavRadioButtonStyle}"/>
                    <RadioButton Content="监控" Tag="&#xE9D2;" Command="{Binding NavigateCommand}" CommandParameter="HardwareMonitor" GroupName="NavGroup" Style="{DynamicResource NavRadioButtonStyle}"/>
                    <RadioButton Content="资源" Tag="&#xE8B7;" Command="{Binding NavigateCommand}" CommandParameter="Resources" GroupName="NavGroup" Style="{DynamicResource NavRadioButtonStyle}"/>
                </StackPanel>

                <!-- Bottom Sidebar -->
                <StackPanel Grid.Row="2" Margin="10,0,10,20">
                     <RadioButton Content="设置" Tag="&#xE713;" Command="{Binding NavigateCommand}" CommandParameter="Settings" GroupName="NavGroup" Style="{DynamicResource NavRadioButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Top Bar -->
                <RowDefinition Height="*"/>    <!-- Content -->
                <RowDefinition Height="Auto"/> <!-- Status Bar -->
            </Grid.RowDefinitions>

            <!-- Top Toolbar -->
            <Border Grid.Row="0" Padding="20,15" Background="#141414" BorderBrush="#2A2A2A" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Logo -->
                    <StackPanel Grid.Column="0" Margin="0,0,30,0" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                            <TextBlock Text="ComfyUI" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"/>
                            <TextBlock Text="{Binding AppName}" FontSize="10" FontWeight="SemiBold" Opacity="0.6" Margin="4,0,0,2" VerticalAlignment="Bottom"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Left: Status Indicator -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border CornerRadius="4" Padding="12,6" Background="#2A2A2A" Margin="0,0,15,0">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="8" Height="8" Margin="0,0,8,0" VerticalAlignment="Center">
                                    <Ellipse.Style>
                                        <Style TargetType="Ellipse">
                                            <Setter Property="Fill" Value="#808080"/> <!-- Default Gray -->
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                                    <Setter Property="Fill" Value="#4CAF50"/> <!-- Green when running -->
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Ellipse.Style>
                                </Ellipse>
                                <TextBlock Text="{Binding ProcessStatusText}" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                            </StackPanel>
                        </Border>

                        <!-- Status Message -->
                        <TextBlock Text="{Binding StatusMessage}"
                                   Foreground="{DynamicResource AccentBrush}"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"
                                   MaxWidth="400"/>
                    </StackPanel>

                    <!-- Right: Action Buttons -->
                    <StackPanel Grid.Column="3" Orientation="Horizontal">
                        <Button Command="{Binding OpenWebPageCommand}"
                                Content="打开"
                                Margin="0,0,10,0"
                                Style="{DynamicResource SecondaryButtonStyle}"/>
                        <Button Command="{Binding ToggleProcessCommand}"
                                Margin="0,0,0,0">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                                    <Setter Property="Content" Value="启动"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                            <Setter Property="Content" Value="停止"/>
                                            <Setter Property="Background" Value="#DC3545"/> <!-- Red -->
                                            <!-- Hover Red -->
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border x:Name="border"
                                                                Background="#DC3545"
                                                                BorderThickness="0"
                                                                CornerRadius="6"
                                                                Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="border" Property="Background" Value="#E04555"/>
                                                            </Trigger>
                                                            <Trigger Property="IsPressed" Value="True">
                                                                <Setter TargetName="border" Property="Background" Value="#C82333"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Content -->
            <ContentControl Grid.Row="1" Content="{Binding CurrentView}" Margin="0"/>

            <!-- Status Bar -->
            <Border Grid.Row="2" Padding="16,8"
                    Background="#0A0A0A">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <Border Background="Transparent" Padding="2,0">
                            <Border.ToolTip>
                                <ToolTip Placement="Top"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         HasDropShadow="False"
                                         MaxWidth="800">
                                    <Border Background="#141414"
                                            BorderBrush="#2A2A2A"
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            Padding="12"
                                            MinWidth="420"
                                            MaxWidth="800">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                                      HorizontalScrollBarVisibility="Disabled"
                                                      MaxHeight="520">
                                            <StackPanel>
                                                <TextBlock Text="ComfyUI system_stats"
                                                           Foreground="{DynamicResource AccentBrush}"
                                                           FontWeight="SemiBold"
                                                           Margin="0,0,0,8"/>
                                                <ItemsControl ItemsSource="{Binding ComfyUiSystemRows}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Margin="0,0,0,4">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="170"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0"
                                                                           Text="{Binding Key}"
                                                                           Foreground="#A0A0A0"
                                                                           FontSize="12"/>
                                                                <TextBlock Grid.Column="1"
                                                                           Text="{Binding Value}"
                                                                           Foreground="#E0E0E0"
                                                                           FontSize="12"
                                                                           TextWrapping="WrapWithOverflow"/>
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>

                                                <TextBlock Text="Devices"
                                                           Foreground="{DynamicResource AccentBrush}"
                                                           FontWeight="SemiBold"
                                                           Margin="0,8,0,6"/>
                                                <ItemsControl ItemsSource="{Binding ComfyUiDeviceRows}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="#101010"
                                                                    BorderBrush="#2A2A2A"
                                                                    BorderThickness="1"
                                                                    CornerRadius="6"
                                                                    Padding="8"
                                                                    Margin="0,0,0,6">
                                                                <StackPanel>
                                                                    <TextBlock Text="{Binding Name}"
                                                                               Foreground="#E0E0E0"
                                                                               FontSize="12"
                                                                               FontWeight="SemiBold"
                                                                               TextWrapping="WrapWithOverflow"
                                                                               Margin="0,0,0,4"/>
                                                                    <Grid Margin="0,0,0,2">
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="70"/>
                                                                            <ColumnDefinition Width="*"/>
                                                                        </Grid.ColumnDefinitions>
                                                                        <TextBlock Grid.Column="0" Text="Type" Foreground="#A0A0A0" FontSize="12"/>
                                                                        <TextBlock Grid.Column="1" Text="{Binding Type}" Foreground="#D0D0D0" FontSize="12" TextWrapping="WrapWithOverflow"/>
                                                                    </Grid>
                                                                    <Grid Margin="0,0,0,2">
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="70"/>
                                                                            <ColumnDefinition Width="*"/>
                                                                        </Grid.ColumnDefinitions>
                                                                        <TextBlock Grid.Column="0" Text="Index" Foreground="#A0A0A0" FontSize="12"/>
                                                                        <TextBlock Grid.Column="1" Text="{Binding Index}" Foreground="#D0D0D0" FontSize="12" TextWrapping="WrapWithOverflow"/>
                                                                    </Grid>
                                                                    <Grid Margin="0,0,0,2">
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="70"/>
                                                                            <ColumnDefinition Width="*"/>
                                                                        </Grid.ColumnDefinitions>
                                                                        <TextBlock Grid.Column="0" Text="VRAM 总量" Foreground="#A0A0A0" FontSize="12"/>
                                                                        <TextBlock Grid.Column="1" Text="{Binding VramTotal}" Foreground="#D0D0D0" FontSize="12" TextWrapping="WrapWithOverflow"/>
                                                                    </Grid>
                                                                    <Grid>
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="70"/>
                                                                            <ColumnDefinition Width="*"/>
                                                                        </Grid.ColumnDefinitions>
                                                                        <TextBlock Grid.Column="0" Text="VRAM 空闲" Foreground="#A0A0A0" FontSize="12"/>
                                                                        <TextBlock Grid.Column="1" Text="{Binding VramFree}" Foreground="#D0D0D0" FontSize="12" TextWrapping="WrapWithOverflow"/>
                                                                    </Grid>
                                                                </StackPanel>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </ScrollViewer>
                                    </Border>
                                </ToolTip>
                            </Border.ToolTip>
                            <TextBlock Text="{Binding StatusBarText}" VerticalAlignment="Center" FontSize="12"/>
                        </Border>
                        <!-- Last Log Line -->
                        <TextBlock Text="{Binding LastLogLine}"
                                   Foreground="#808080"
                                   FontFamily="Consolas, Courier New, Monospace"
                                   FontSize="12"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"
                                   Margin="20,0,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding LastLogLine}" Value="">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                         <TextBlock Text="{Binding AppVersionText}" Opacity="0.6" VerticalAlignment="Center" FontSize="12"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
